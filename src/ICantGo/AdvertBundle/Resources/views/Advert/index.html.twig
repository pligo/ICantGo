{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Advert list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Issold</th>
                <th>Idconcert</th>
                <th>Description</th>
                <th>Publishedat</th>
                <th>Price</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('advert_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.isSold }}</td>
                <td>{{ entity.idConcert }}</td>
                <td>{{ entity.description }}</td>
                <td>{% if entity.publishedAt %}{{ entity.publishedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.price }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('advert_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('advert_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('advert_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
